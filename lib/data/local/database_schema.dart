const List<String> schemas = [
  ''' CREATE TABLE Users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    email TEXT UNIQUE,
    password TEXT NOT NULL,
    currency TEXT DEFAULT "BOB",
    createdAt TEXT NOT NULL,
    updatedAt TEXT NOT NULL
  )''',
  '''CREATE TABLE Accounts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    accountType TEXT NOT NULL CHECK(accountType IN ('checking','saving','credit_card','cash','investment')),
    balance REAL DEFAULT 0.0,
    currency TEXT DEFAULT "BOB",
    icon TEXT NOT NULL,
    color TEXT NOT NULL DEFAULT 'FF14b6eb',
    isActive BOOLEAN DEFAULT 1,
    createdAt TEXT NOT NULL,
    updatedAt TEXT NOT NULL,
    userId INTEGER NOT NULL,
    FOREIGN KEY (userId) REFERENCES Users(id) ON DELETE CASCADE
  )''',
  ''' CREATE TABLE Categories(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    categoryType TEXT NOT NULL CHECK(categoryType IN ('expense','income')),
    color TEXT DEFAULT 'FF14b6eb',
    icon TEXT NOT NULL,
    isActive BOOLEAN DEFAULT 1,
    createdAt TEXT NOT NULL,
    updatedAt TEXT NOT NULL,
    parentCategoryiD INTEGER,
    userId INTEGER,
    FOREIGN KEY (userId) REFERENCES Users(id) ON DELETE CASCADE,
    FOREIGN KEY (parentCategoryiD) REFERENCES Categories(id) ON DELETE SET NULL
  )''',
  '''CREATE TABLE Trans(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    transactionType TEXT NOT NULL CHECK(transactionType IN ('income','expense','transfer')),  
    amount REAL NOT NULL CHECK(amount >= 0),
    description TEXT,
    notes TEXT,
    transactionDate TEXT NOT NULL,
    recipeImagePath TEXT,
    location TEXT,
    transferToAccountId INTEGER,
    isRecurring BOOLEAN DEFAULT 0,
    recurringTransactionId INTEGER,
    createdAt TEXT NOT NULL,
    updatedAt TEXT NOT NULL,
    userId INTEGER,
    accountId INTEGER,
    categoryId INTEGER,
    FOREIGN KEY (userId) REFERENCES Users(id) ON DELETE CASCADE,
    FOREIGN KEY (accountId) REFERENCES Accounts(id) ON DELETE SET NULL,
    FOREIGN KEY (categoryId) REFERENCES Categories(id) ON DELETE SET NULL,
    FOREIGN KEY (transferToAccountId) REFERENCES Accounts(id) ON DELETE SET NULL,
    FOREIGN KEY (recurringTransactionId) REFERENCES Trans(id) ON DELETE SET NULL
  )''',
  '''CREATE TABLE RecurringTransactions(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    transactionType TEXT NOT NULL CHECK(transactionType IN ('income','expense','transfer')),
    amount REAL NOT NULL CHECK(amount >= 0),
    description TEXT,
    notes TEXT,
    frequency TEXT NOT NULL CHECK(frequency IN ('daily','weekly','monthly','yearly')),
    startTime TEXT NOT NULL,
    endTime TEXT,
    startDate TEXT NOT NULL,
    endDate TEXT,
    nextExecutionDate TEXT NOT NULL,
    transferToAccountId INTEGER,
    isActive BOOLEAN DEFAULT 1,
    createdAt TEXT NOT NULL,
    updatedAt TEXT NOT NULL,
    userId INTEGER,
    accountId INTEGER,
    categoryId INTEGER,
    FOREIGN KEY (userId) REFERENCES Users(id) ON DELETE CASCADE,
    FOREIGN KEY (accountId) REFERENCES Accounts(id) ON DELETE CASCADE,
    FOREIGN KEY (categoryId) REFERENCES Categories(id) ON DELETE RESTRICT,
    FOREIGN KEY (transferToAccountId) REFERENCES Accounts(id) ON DELETE SET NULL
  )''',
  '''CREATE TABLE Budgets(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    amount REAL NOT NULL CHECK(amount >= 0),
    period TEXT NOT NULL CHECK(period IN ('weekly','monthly','yearly')),
    startDate TEXT NOT NULL,
    endDate TEXT NOT NULL,
    spendAmount REAL DEFAULT 0.0,
    isActive BOOLEAN DEFAULT 1,
    createdAt TEXT NOT NULL,
    updatedAt TEXT NOT NULL,
    userId INTEGER,
    categoryId INTEGER,
    FOREIGN KEY (userId) REFERENCES Users(id) ON DELETE CASCADE,
    FOREIGN KEY (categoryId) REFERENCES Categories(id) ON DELETE CASCADE
  )''',
  '''CREATE TABLE Goals(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    description TEXT,
    targetAmount REAL NOT NULL CHECK(targetAmount >= 0),
    currentAmount REAL DEFAULT 0.0,
    targetDate TEXT NOT NULL,
    goalType TEXT CHECK(goalType IN ('savings','debPayment','purchase')),
    isCompleted BOOLEAN DEFAULT 0,
    createdAt TEXT NOT NULL,
    updatedAt TEXT NOT NULL,
    userId INTEGER,
    categoryId INTEGER,
    FOREIGN KEY (userId) REFERENCES Users(id) ON DELETE CASCADE
  )''',
];
